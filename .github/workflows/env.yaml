name: My Environment

on: workflow_dispatch

env:
  APP: MyApp

jobs:

  job1:
    runs-on: ubuntu-latest

    env:
      APP: DevOps

    steps:
      - name: Step 1
        run: echo "My App is $APP"

      - name: Step 2
        env:
          APP: GitHub Actions
        run: echo "My App is $APP"

  job2:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "My App is $APP"
